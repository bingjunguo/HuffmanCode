--Assignment3为第5题，其中以“_com”结尾的是压缩后的文件，以“_decom”结尾的是
解压缩后的文件。请运行HuffmanCode中的main方法，实现对两个文件的压缩和解压缩。
----类HuffmanCode，中实现了压缩和解压缩两个方法，并在控制台打出了相关结果。
----类MinHeap为小根堆，作为哈夫曼压缩算法中的优先队列。
----类HuffNode为哈夫曼树中的节点。
----类BitOutputStream、BitInputStream为按位输入输出辅助类。